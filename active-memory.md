## 时间：2026-05-09 14:32:53

> 原始问题：这里还有一个问题。就是如果文件已经有了。我们往下新增的时候要首先在上面加个----- 这样的分割线。然后空格 在写入记录。不然会看起来都融在一起。分不清 那块记录

## 1. 给 active-memory 追加记录时增加分割线

## 描述：

### 1. 确认了需求是只调整 `kc-am` 的追加格式，不改其他技能逻辑。

### 2. 把 `active-memory` 技能规则改成：当 `active-memory.md` 已有内容时，先写一行单独的 `-----`，再空一行，再追加新的记录内容。

### 3. 明确要求分割线必须独占一行，避免前后两条记录视觉上粘连，后续阅读时更容易区分每块记忆。

### 4. 重新执行了 `./scripts/sync.sh`，把更新后的 `kc-am` 规则重新生成并安装到本地技能目录。
