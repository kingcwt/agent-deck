# `source.md` 中文对照说明

本文件与 [source.md](./source.md) 一一对应。

约束说明：

- `source.md` 是唯一正式源文件，也是分发、渲染、安装时使用的英文源文件。
- 本文件只用于中文理解、学习和阅读，不用于渲染或安装。
- 不允许在本文件里单独新增一套逻辑。
- 如果 `source.md` 更新，本文件必须同步做对应翻译更新。
- 如果中文内容与英文源文件冲突，以 `source.md` 为准。

---

## Frontmatter 对照

### `name`

`git-diff-description`

技能名，保持英文短名，不翻译。

### `display_name`

`Git Diff Description`

显示名称，表示“Git 改动描述”。

### `description`

含义：

基于每个文件的 git diff 和周边上下文，逐个为当前仓库里的改动文件生成简短、具体的描述。适用于用户输入 `/kc-gdd`、`$kc-gdd`、`kc-gdd`，或者明确要求“找到当前所有改动文件，并按文件输出一句简短说明这次改了什么”的场景。

### `short_description`

`Summarize each changed file in one short, concrete line`

含义：

为每个改动文件输出一行简短且具体的总结。

### `default_prompt`

`Use $kc-gdd to find all current changed files and output one short, concrete description for each file based on its diff and context.`

含义：

使用 `$kc-gdd` 找到当前所有改动文件，并基于各自的 diff 和上下文，为每个文件输出一句简短、具体的描述。

### `codex_names`

`kc-gdd`

表示 Codex 侧生成一个技能别名：`kc-gdd`。

### `claude_skill_names`

`kc-gdd`

表示 Claude skill 侧生成一个技能别名：`kc-gdd`。

### `claude_commands`

`kc-gdd`

表示 Claude command 侧生成一个命令别名：`/kc-gdd`。

### `allow_implicit_invocation`

`false`

表示默认不允许隐式注入，优先要求显式调用。

---

# Git Diff Description

## Overview

检查当前仓库里的所有改动文件，逐个读取每个文件的 diff，并补充理解该改动所需的最小上下文，然后为每个文件输出一句简短、具体的描述。目标是说清楚“做了什么”，而不是写审查意见、commit message 或长篇解释。

## Workflow

### 1. 先找出所有改动文件

- 把 `/kc-gdd` 和 `$kc-gdd` 视为这个工作流的显式快捷触发词。
- 从当前仓库状态开始，在写任何描述之前先识别出所有改动文件。
- 包含 staged 和 unstaged 的已跟踪改动，也包含当前工作区里的 untracked 文件。
- 保持文件级粒度，主要输出单位是“每个改动文件一条描述”。
- 如果没有任何改动文件，就直接说明并停止。

### 2. 结合足够上下文阅读 diff

- 对每个改动文件，先检查它自己的 git diff。
- 如果仅看 diff 还不足以判断改动内容，就继续阅读该文件当前内容。
- 只补充理解这个改动所必需的最近邻代码或引用，例如相关组件、类型、工具函数或调用点。
- 对于 untracked 文件，直接读取文件内容，并结合文件名和内容推断它的用途。
- 对于被删除的文件，结合删除 diff 和周边引用说明删掉了什么。

### 3. 每个文件写一句简短描述

- 除非用户明确要求更细，否则每个改动文件只输出一条主描述。
- 每条描述都要短、具体、以动作表达为主。
- 优先使用类似 `新增登录按钮`、`补充 UserSession 类型`、`调整订单列表请求参数`、`删除废弃支付回调` 这样的表述。
- 要点名真正变动的功能、类型、字段、组件、配置或行为，避免 `优化代码`、`修复一些问题`、`更新逻辑` 这类空泛说法。
- 如果一个文件里有多个彼此强相关的改动，要压缩成一句能表达主意图的短句。
- 如果无法完全确定真实意图，要基于代码谨慎表述，例如写成“看起来是”“大概率用于”“用于补充”。

### 4. 保持输出紧凑

- 默认使用扁平的逐文件列表格式，形如 `path: 简短描述`。
- 描述要尽量短，让用户能快速扫完整个改动集。
- 除非用户明确要求，否则不要展开成长解释、审查结论或逐步推理。
- 当用户要求逐文件描述时，不要把多个文件压缩成一个整仓库总述。

## Guardrails

- 这是一个“简短描述 diff”的工作流，不是代码审查、代码修改或提交代码的工作流。
- 只要 diff 或文件内容可读，就不要只凭文件名编造描述。
- 代码里有明确业务名词或技术名词时，不要退化成泛化标签。
- 除非用户明确缩小范围，否则不要静默跳过改动文件。
- 如果无法高置信判断文件用途，要简短说明不确定性，不要把推断说成确定事实。
